{
    "changes": [
        {
            "packageName": "@gooddata/sdk-ui-all",
            "comment": "sdk-ui-ext: Fix useObjectAccessLevel matching the caller's own grant, which never hit because the profile ref is a URI while access list grants carry the user id.",
            "type": "none"
        },
        {
            "packageName": "@gooddata/sdk-ui-all",
            "comment": "sdk-ui-ext: Add the selfGranteeRef and summaryOtherGranteeCount helpers, so a consumer fetching its own access list can discount the caller's grant the same way the share dialog does.",
            "type": "none"
        }
    ],
    "packageName": "@gooddata/sdk-ui-all"
}
