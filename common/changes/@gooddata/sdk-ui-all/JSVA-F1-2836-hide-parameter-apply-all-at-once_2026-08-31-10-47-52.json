{
    "changes": [
        {
            "packageName": "@gooddata/sdk-ui-all",
            "comment": "sdk-ui-kit: `ParameterControl` now takes an explicit submit mode: `mode: \"commit\"` with `onCommit`, or `mode: \"staged\"` with `onStage`, plus one `onClose`. In commit mode, the Apply button commits and the control closes itself; in staged mode, every valid draft is staged immediately, allowed-value picks stage and close, and the footer shows one Close button. The control also closes itself after an allowed-value pick in commit mode.",
            "type": "none"
        },
        {
            "packageName": "@gooddata/sdk-ui-all",
            "comment": "sdk-ui-dashboard: In apply-all-at-once mode, all parameter types stage their value as the working value. Apply All then commits the value. No parameter dropdown shows an Apply button. If you type a value, the dropdown stages it as soon as the value is valid, and the dropdown stays open.",
            "type": "none"
        }
    ],
    "packageName": "@gooddata/sdk-ui-all"
}
