{
  "_note": "This config is used only for storybook/ directory to add typings.d.ts.",
  "extends": "../tsconfig.base.json",
  "include": [
    "../src/typings.d.ts",
    "./*.ts",
    "./*.tsx"
  ]
}
