# This config file overrides _default/config.toml
# if hugo builds site for the environment "public"
# (hugo -e public ...)

# This contentDir is empty dir in Git. It needs to be
# generated by an external script.
# contentDir = "multidocs"
