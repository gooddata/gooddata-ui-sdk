version: "3.7"
services:

  scenarios-local:
    container_name: scenarios-local
    build:
      context: .
      dockerfile: ./scenarios/Dockerfile
    volumes:
      - "./scenarios/build:/app"
    ports:
      - 3001:3000


