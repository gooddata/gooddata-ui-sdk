# (C) 2019-2022 GoodData Corporation

# IMPORTANT! Do not keep comments on the same line as values,
# and do not use spaces around =.
# Correct:   FIXTURE_TYPE=goodsales
# Incorrect: FIXTURE_TYPE=goodsales #goodsales|demo
# Incorrect: FIXTURE_TYPE = goodsales

# Tiger instance URL
HOST=https://staging.dev-latest.stg11.panther.intgdc.com

# API token for authentication
TIGER_API_TOKEN=

# Fixture type: goodsales | demo
FIXTURE_TYPE=goodsales

# Data source name: demo-ds | pg_staging-demo | vertica_staging-goodsales
TIGER_DATASOURCES_NAME=vertica_staging-goodsales

# Workspace ID (set automatically by create-ref-workspace / update-ref-workspace)
TEST_WORKSPACE_ID=

# Playwright grep filter for integrated tests (e.g. @post-merge-fe)
# Not needed for isolated tests (hardcoded to @pre-merge-isolated)
PLAYWRIGHT_GREP=

# Filter by spec file name (e.g. embedDialog.spec.ts)
FILTER=
